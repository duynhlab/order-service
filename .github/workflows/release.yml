name: Release

# Publishes the order-service release binary via GoReleaser on a `v*` tag.
on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    uses: duynhlab/gha-workflows/.github/workflows/goreleaser.yml@main
    secrets: inherit
